const LoginInitState = {}

export const loginReducer = (state = LoginInitState, action: LoginActionsType) => {
    switch (action.type) {
        default:
            return state
    }
};

//actions
type LoginStateType = any
type LoginActionsType = any