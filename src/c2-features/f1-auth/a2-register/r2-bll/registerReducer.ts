const RegisterInitState = {}

export const registerReducer = (state = RegisterInitState, action: RegisterActionsType) => {
    switch (action.type) {
        default:
            return state
    }
};

//actions
type RegisterStateType = any
type RegisterActionsType = any