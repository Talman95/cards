const ForgotInitState = {}

export const forgotReducer = (state = ForgotInitState, action: ForgotActionsType) => {
    switch (action.type) {
        default:
            return state
    }
};

//actions
type ForgotStateType = any
type ForgotActionsType = any