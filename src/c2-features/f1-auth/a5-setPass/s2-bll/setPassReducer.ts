const SetPassInitState = {}

export const setPassReducer = (state = SetPassInitState, action: SetPassActionsType) => {
    switch (action.type) {
        default:
            return state
    }
};

//actions
type SetPassStateType = any
type SetPassActionsType = any